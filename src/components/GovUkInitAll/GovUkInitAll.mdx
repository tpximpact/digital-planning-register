import { Meta } from "@storybook/blocks";

<Meta title="Utils/GovUkInitAll" />

# GovUkInitAll

Since the govuk toolkit isn't designed to be used in a react application we're initiating it through this component. Its not perfect but it covers the basic use cases. In case of more advanced functionality eg accordions we should write our own code.
