name: Accessibility Tests

on:
  pull_request:
    branches: [main]

jobs:
  pa11y:
    runs-on: ubuntu-latest

    container:
      image: buildkite/puppeteer:10.0.0

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build the application
        run: NODE_ENV=test NEXT_PUBLIC_API_MOCKING=enabled npm run build

      - name: Start the application
        run: NODE_ENV=test NEXT_PUBLIC_API_MOCKING=enabled npm start & echo $! > pid.txt

      - name: Wait for the application to start
        run: npx wait-on http://localhost:3000

      - name: Run pa11y-ci
        run: |
          npm install -g pa11y-ci
          NODE_ENV=test NEXT_PUBLIC_API_MOCKING=enabled pa11y-ci --config .pa11yci

      - name: Stop the application
        run: kill $(cat pid.txt)

      - name: Log environment for debugging
        run: |
          echo "NEXT_PUBLIC_API_MOCKING: $NEXT_PUBLIC_API_MOCKING"
          echo "NODE_ENV: $NODE_ENV"
